local rax_array = {
    "%al",  -- 8-bits register
    "%ax",  -- 16-bits register
    "%eax", -- 32-bits register
    "%rax", -- 64-bits register
}

local x = 19
if x <= 3 or 1 ~= 2 then print("Hello world") end
